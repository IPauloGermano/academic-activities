# 3G

hr_trabalhadas = float(input('Digite o número de horas trabalhadas: '))
valor_hora = float(input('Digite o valor da hora trabalhada: '))

salario_total = hr_trabalhadas * valor_hora

print(f'O salário total é: R$ {salario_total:.2f}')